# Optional local overrides for systemd EnvironmentFile.
# Copy to ~/.config/local-inference/local-inference.env and fill locally.

# LOCAL_INFERENCE_DATA_ROOT=/path/to/local-inference-data
# LOCAL_INFERENCE_MODELS_DIR=/path/to/local-inference-data/models
# LOCAL_INFERENCE_VENV=/path/to/local-inference-data/.venv-local-inference
# LOCAL_INFERENCE_CPU_RESERVED_THREADS=2

# PaddleOCR media/document OCR serving is owned by local-inference.
# LOCAL_INFERENCE_PADDLEOCR_HOME=/path/to/local-inference-data/paddleocr
# LOCAL_INFERENCE_PADDLE_PDX_CACHE_HOME=/path/to/local-inference-data/paddleocr/paddlex-cache
# LOCAL_INFERENCE_PADDLEOCR_CONCURRENCY=1
# LOCAL_INFERENCE_PADDLEOCR_QUEUE_LIMIT=8
# PADDLE_PDX_ENABLE_MKLDNN_BYDEFAULT=0

# Optional candidate/evaluation model downloads managed by local-inference.
# LOCAL_INFERENCE_CANDIDATE_MODELS_DIR=/path/to/local-inference-data/models/candidates
# LOCAL_INFERENCE_CANDIDATE_MODEL_LOG_DIR=/path/to/local-inference-data/logs/candidate-model-downloads
# LOCAL_INFERENCE_CANDIDATE_MODEL_MANIFEST=/path/to/candidate-models.tsv
# LOCAL_INFERENCE_CANDIDATE_DOWNLOAD_JOBS=4

# Hugging Face auth is optional for public models and should come from env.
# HF_TOKEN=
# HF_TOKEN_FILE=
